{
  "name": "static-site",
  "version": "1.0.0",
  "description": "Creates a README file by using a command-line application to generate one.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^8.2.6"
  }
}
